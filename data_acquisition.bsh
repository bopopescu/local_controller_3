
while [ 1 ]
do
   echo "executing data_acquisition interface module"
   python data_acquisition.py >>  /tmp/data_acquisition.err 2>&1
   python cloud_event_queue.py "reboot" "data_acquisition"
   sleep 10
   echo "dumping error file"
   mv /tmp/data_acquisition.err /tmp/data_acquisition.errr

done
